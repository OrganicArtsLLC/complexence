# Tier Fix Pass — 2026-07-01

Applied against the three same-day reviews (theory-algebra, implementation-integrity,
voice-filters). **Nothing committed, nothing pushed** — every change sits in the
working tree for Joshua's read-through. The three quoted blocks at the bottom are the
pieces to read before any push decision.

---

## 1. Applied

### The centerpiece — the honest v2 writeup (theory H1, integrity B-1/B-2)

| ID | File | Where | What |
|---|---|---|---|
| H1/B-2 | `ROADMAP.md` | :97-122 (new section) | "Second experiment result (v0.2) — invalid run, raw direction against the prediction": pre-registration restated, refusal-contamination invalidation (incl. the rubric-1.0 refusal), the adverse raw deltas (−0.12 / −0.38), what changes before the re-run. Status line (:9-13) updated to point at it. |
| H1/B-2 | `src/README.md` | :86-108 (new section) | "Lossy-channel experiment — `experiment_v2.py`" with the same honest summary + pointer to ROADMAP. |
| B-1 | `src/experiment_v2.py` | :24-26 docstring | "committed before the run — see git history" → "written in this docstring before the run; the run artifacts are gitignored, so the ordering is not verifiable from git history — stated for what it is." Verdict comment (:~220) matched. Added a FIRST-RUN NOTE to the docstring. |
| M7 (bug fix) | `src/experiment_v2.py` | new `REFUSAL_SIGNATURES` / `invalid_output()` / `InvalidTrial`; `relay()` checks every hop; `run_cell()` returns `valid: False` instead of scoring; `summarize()` means over valid trials only, treats NaN as missing (also closes L8/B-7 NaN nit), reports `n_valid/n_total` per cell; console prints validity and a NO-VERDICT branch when cells can't be compared. |
| M7 (bug fix) | `src/demo.py` | `claude()` adapter | raises on nonzero exit instead of returning stderr-shaped stdout as output. |

### Theory fixes (reframe, never inflate)

| ID | File | Where | What |
|---|---|---|---|
| H2 | `spec/complexence-science.md` | :456-465 (§7.1 notation note) | The `⊂`-on-a-tuple / `⋂`-without-carrier problem named in place; formulas marked **candidate notation**; minimal intended reading given (`Sem`, `Meaning: representations → 𝒫(Sem)`, `P_i`); debt routed to §10.6. |
| H2 | `spec/complexence-science.md` | :753 (§10 item 7) | Red-team B1 fine print imported into the debts ledger (quoted below). |
| H3 | `spec/complexence-science.md` §5.4 + `spec/complexence-capability.md` :21-26 | both | "the equation defines what 'well' means precisely" → the equation states the loop formally; "well" is the open measurement question of Theory A (§6, §10.5). |
| H4 | `spec/complexence-science.md` | :544-550 (§7.6) | Shape-identity "evidence" → Φ_A/Φ_B distinguished, shared symbol called aspirational, resemblance demoted to what *motivates* the §10.2 conjecture; "every discrete dynamical system has this shape" stated. |
| H5 | `spec/complexence-science.md` | :519-525 (§7.4) + :761 (§10 item 8) | E12 resolution adopted: clarity = confidence in the surviving core (rises, under independence), not content (weakly shrinks); correlated-projection caveat included; logged in §10 as a recorded contradiction pending formalization (quoted below). |
| M6 | `src/cognitive_form.py` | `compose()` | unions feedback_loops, appends a history entry with both parents' content hashes (`_form_hash`), derives a fresh id (`uuid5` of the merged essence); non-commutativity stated in the docstring. Verified: composed form validates against the schema. |
| M8 | `spec/complexence-science.md` | :96 (§2 stage table) | Gödel → Self-reference. |
| M5 | `ROADMAP.md` :57-61, `src/example.cform.json` :29, `proofs/channel-capacity-sketch.md` :7 | "external review" → solicited, model-assisted feedback, author-vetted; "genuine external review is still pending" said out loud. |
| M4 | `spec/complexence-science.md` | :608-621, :642-650 (§8.5) | "exact rather than poetic" → "uses the calculus's own operators… at whatever precision those operators currently have"; looseness-as-guaranteed-residue → the two facts separated, resolution assigned to the §10 work, not the axiom; "the loop's own requirement" → conditional recommendation (*if* Law 3 holds) + "ordinary good reasons that need no theory." |
| B-5 (bonus, concrete fix given) | `spec/complexence-science.md` | §8.4 | OS "engineering instantiation" overclaim → OS is markdown + prompt contracts today; `src/` named as the current partial instantiation; runtime marked aspiration. |

### Integrity / privacy

| ID | File | What |
|---|---|---|
| B-3 | `README.md` :181-186, new `src/LICENSE` (copy of `scripts/LICENSE`), license lines added to `demo.py`, `experiment.py`, `experiment_v2.py` docstrings | License split now consistent: code (`scripts/`, `src/`) MIT; prose CC BY 4.0. |
| P-1 | `spec/complexence-capability.md` §10 | Dead private `sources/*` filename list deleted → "Distilled from private research notes and drafts (not published)." |
| P-2 | `spec/complexence-capability.md` :131, `spec/complexence-science.md` :650 | "blog ADR-023" → "kept in a private strategy record." |
| B-4 | `README.md` :140-153, :163-167 | Layout block gains `src/` and `proofs/` rows; "and nothing else" → "plus the science's early artifacts (`src/`, `proofs/`) — and nothing private, nothing filled in." |
| B-6 (bonus) | `src/cognitive_form.py` CLI | stray docstring line in usage output dropped. |

### Voice (all six HIGHs + the concrete MEDIUM/LOWs)

| ID | File | What |
|---|---|---|
| F1-H README front door | `README.md` :3-8 | Rewritten analogy-first, first person, lived ground (quoted below). |
| F1-H Cognitive Form first use | `ROADMAP.md` :23-25; `spec/complexence-science.md` §1 | One-breath plain gloss at both first-contact points; "representation-invariant" defined in the ROADMAP gloss. |
| F1-H AST | `spec/complexence-science.md` :96 | "(AST = abstract syntax tree — the structure a compiler keeps after it throws away your exact wording)". |
| F1-H OODA | `spec/complexence-science.md` :172 | "OODA (Boyd's observe–orient–decide–act loop, from air combat)." |
| F3-H "Most people" | `docs/layers.md` :140 | → "You can get enormous value…" |
| F3-H "drew external review" | `ROADMAP.md` :57-61 | Honest verbs (same edit as M5). |
| F1-M L0–L7 / foundation-row jargon | `README.md` :13-15 | Both table cells glossed (eight-level map; ontology/equation/meaning velocity in plain words). |
| F1-M MVP | `spec/complexence-os.md` :167, `docs/instantiate.md` :57 | "MVP (minimum viable product)". |
| F1-M LLM / F1-L headless | `docs/automation.md` :31, :38; `spec/complexence-science.md` §6.7 | Expanded at first use. |
| F2-M "It isn't. It's Y" | `docs/layers.md` :3-5 | → "…and it is one. It's also a ladder…" (holds the both/and). |
| F2-M "thus not X but Y" | `spec/complexence-science.md` :642-646 | Rewritten (inside the M4 edit). |
| F2-M floating tagline | `spec/complexence-science.md` :578-581 | → "The short version I keep coming back to: …" (also breaks one triad). |
| F2-M "just a brand" ×2 | `ROADMAP.md` :32-33 | First instance → "because the open edges *are* the field"; the earned second instance kept; the new v0.2 section deliberately does **not** re-use it. |
| F2-M em-dash sweep | `README.md` (24→~14), `docs/layers.md` (29→11), `ROADMAP.md` (~31→22, prose parentheticals only) | Converted to commas/parens/colons/second sentences; headings and definition-list dashes left (house style). |
| F2-L leverage density | `README.md`, `docs/layers.md` | "payoff," "the gain is real," "Fixes compound." |
| F2-L bolded declarations | `docs/layers.md` :59, :91 | Unbolded/folded ("This is separation of concerns, applied to thought."). |
| F3-M field-ladder grounding | `spec/complexence-science.md` :126-127 | "That's the claim, and it's one person's bet, not a consensus — §3a says what the neighbors already cover, and §10 says what this still owes." |
| F4-M "executed and checked" | `src/README.md` :81-84 | → "run end-to-end and judged by a model, once. Not proof; motion." |
| F4-L "is validated with numbers" | `spec/complexence-os.md` :215 | → "can be checked with numbers." |
| F4-L "exact rather than poetic" | `spec/complexence-science.md` :610 | (inside M4). |

### The outbox note (the coherence-steward pattern, private instance, staged)

- Draft-gate header block stripped.
- "Every growing multi-agent system hits this wall." → "Any system I've grown this way has hit this wall, and the failure mode is general: …"
- "This holds across structurally different domains" → "The same shape keeps showing up across structurally different domains" (adjacent "same shape" de-duplicated to "the same").
- Left as-is per the review's own verdict: "Coherence is not a byproduct. It is a role." (flagged fine-to-leave). Destination path + README listing = Joshua's push-time call.

**Applied: 38 findings** (5 theory HIGH, 5 theory MEDIUM incl. two code fixes, 2 theory LOW folded in (L5, L8-NaN), 3 integrity HIGH, 2 integrity MEDIUM (B-4, B-5), 2 integrity LOW (B-6, seed-note from B-7), 2 privacy LOW, 6 voice HIGH, 8 voice MEDIUM, 5 voice LOW, 3 outbox edits).

---

## 2. Skipped (and why)

| ID | Finding | Why skipped |
|---|---|---|
| Theory M1 | §6.4 operator algebra (⊗, ⊕ carriers; A(A) typing) | Not in the mandated fix list; needs a real design decision (product space, order, reflexive embedding), not a wording pass. |
| Theory M2 | §6.6 `S` vs proofs' `Φ` collision | Not in the mandated list; one-symbol fix but touches the flagship bound + the Φ-collision ledger (L1) — cleaner done together, deliberately. |
| Theory M3 | §7.5 "The laws" unlabeled / Law 3 pseudo-equation | Not in the mandated list; retitling the laws is a doctrine decision. |
| Theory M9 | "conjectured axioms" wording / vacuous trust bound | Not in the mandated list. |
| Theory L1–L4, L6, L7 | Φ triple-duty, "conservation" inequality, exponential form, deterministic-id promise, proof-sketch A4/τ, kernel double-pass | Nits needing spec-owner judgment; L4 partially mitigated (compose now derives a uuid5 id, but the schema description still overpromises). |
| Integrity B-8 | GUARDRAILS "no real names" vs public attribution | One-line rule rescope, but GUARDRAILS is the safety text — changed only with Joshua's eyes on it. |
| Integrity B-9 | `assets/census` vs `census.md` convention drift | Contract-identity rule says pick one convention everywhere; that's a cross-file rename decision. |
| Integrity B-7 (part) | experiment.py `list[int]`/None annotation; re-seed on `(p, trial)` | Behavior-neutral nits; the seed asymmetry is now documented in a comment instead of changed. |
| Voice F1-L | Greek loop early in capability spec | Review's own verdict: fine to leave. |
| Voice F2-L | repeated seed tagline; README:55 mirrored pair; remaining triads | Review calls the tagline defensible as-is; the rest individually harmless. |

---

## 3. The pieces Joshua must read before any push

### 3a. The new v0.2 writeup — `ROADMAP.md`

> ### Second experiment result (v0.2) — invalid run, raw direction against the prediction
>
> The v0.2 docstring pre-registered a signature for the bet: A and B tie on rubric at
> p=0.0, and A beats B by ≥ +0.20 at p=0.6, the delta growing with noise. The run
> happened on 2026-06-28. It did not deliver a valid test, and what it did produce
> leaned the other way. Both facts belong here, in order of importance:
>
> 1. **The run is invalid.** At p=0.6 the model adapter returned API-refusal text
>    ("Claude Code is unable to respond to this request…") instead of elaborations.
>    The refusals entered the relay as if they were work products, then went to the
>    judges as if they were finals; one pure refusal was scored **rubric 1.0**, a
>    graded artifact expressing nothing. The v0.1 lesson repeated one layer down: I
>    hardened the scorer, and then the *harness* fed it garbage it trusted. §10.5 in
>    practice, again: measurement validity is the whole bet.
> 2. **For the record, the raw deltas ran against the pre-registered signature:**
>    rubric delta A−B was **−0.12 at p=0.0 and −0.38 at p=0.6** — the anchored
>    condition did *worse* as the channel degraded, the inverse of the prediction.
>    Because of (1), this is not clean evidence against the bet; it is an invalid run
>    whose direction happened to be adverse. It is reported anyway, because a public
>    pre-registration with a silent outcome is worse than no pre-registration at all.
>
> What changes before the re-run: the harness now rejects refusal/error outputs (a
> nonzero adapter exit raises; a refusal signature invalidates the trial instead of
> scoring it), and every cell reports its validity rate. Then v0.2 runs again, clean.
> Until that lands, the pre-registration stands as an open debt, and this section is
> the receipt.

And the reframed "Landed so far" opening (same file):

> The feedback the §8.5 argument predicts started running within hours of going
> public. Honest framing: the first outside passes came from me putting the spec in
> front of other frontier models and vetting what came back — solicited, model-assisted
> feedback, not community traction. Genuine external review (minds not under my
> direction) is still pending, and saying so is the point:

### 3b. The v2 writeup — `src/README.md`

> ## Lossy-channel experiment — `experiment_v2.py`
>
> v0.2 isolates the variable the v0.1 null pointed at: the channel. The same relay,
> but the text passed between hops goes through word-dropout at rate `p`; condition A
> re-pins the full invariant clean at every hop, B gets only the degraded previous
> text. Fidelity is triangulated three ways (per-claim rubric, reconstruction, holistic
> judge). The docstring carries a pre-registered prediction, written before the run;
> the run artifacts are gitignored, so the ordering is stated, not provable from git
> history.
>
> ```bash
> python experiment_v2.py --hops 5 --trials 2 --noise 0.0,0.6
> ```
>
> **The first v0.2 run was invalid, and its raw direction went against the prediction.**
> At p=0.6 the adapter returned API-refusal text, refusal text flowed through the relay
> and into the judges, and one pure refusal was scored rubric 1.0. The raw rubric deltas
> (A−B −0.12 at p=0.0, −0.38 at p=0.6) ran opposite the pre-registered signature — but
> the contamination means this is an invalid run whose direction happened to be adverse,
> not clean evidence against the bet. The harness now raises on adapter errors,
> invalidates (rather than scores) refusal outputs, and reports a per-cell validity
> rate; a clean re-run is the open next step. Full write-up in
> [`../ROADMAP.md`](../ROADMAP.md).

### 3c. The README front door

> **Complexence is a word I made up for something specific: staying oriented inside
> a system too big to hold in your head — a codebase, a job, a life — and turning
> that orientation into action you'd stand behind.** This repo holds the map of
> that capability, the working method I run it with, and the early, unproven
> science underneath. One word, three layers of depth, one versioned repo.

### 3d. The debts-ledger additions — `spec/complexence-science.md` §10.6 (items 7–8)

> 7. **The central law is not yet well-formed notation.** ℭ is a tuple, so
>    `R_i(ℭ) ⊂ ℭ` uses `⊂` with no defined sense on tuples, and
>    `I(ℭ) = ⋂_i Meaning(R_i)` needs a constructed common carrier before the
>    intersection means anything. The intended reading is stated in the §7.1
>    notation note (a carrier `Sem` of consequences-for-action, with
>    `Meaning : representations → 𝒫(Sem)`); building `Sem` is the debt. Until then
>    the three flagship formulas (§7.1, §7.2, §7.4's `Model(ℭ) ⊂ ℭ`) are candidate
>    notation, not set theory.
> 8. **Intersection vs. clarity — an open tension in Theory B's core.** The
>    identity `I(ℭ)` weakly *shrinks* as valid projections are added (that is what
>    intersections do), while multiprojection clarity says a form gets *clearer*
>    with more consistent projections. The working resolution, stated in §7.4 and
>    not yet formalized: added projections sharpen *confidence* in the surviving
>    core, not its *content* — and only under projection independence, which is
>    exactly what fails when the projections share an author or a model family.
>    Until formalized, this stands as a recorded contradiction, not a theorem.

And the new §7.1 notation note they anchor to:

> **Notation note (read with §1a).** ℭ is a tuple, not a set, so as written `⊂` has
> no defined sense here and the `⋂` in §7.2 has no constructed carrier — these
> formulas are **candidate notation**: a picture of the intended claim, not yet set
> theory. The intended reading, stated so the debt is payable: let `Sem` be a set of
> *consequences for action under a goal*; let `Meaning : representations → 𝒫(Sem)`;
> read `R_i ⊂ ℭ` as "`R_i` is a projection of ℭ that never exhausts it"
> (`P_i(ℭ) = R_i` for some projection map `P_i`); then `I(ℭ) := ⋂_i Meaning(R_i)` is
> a definition in `𝒫(Sem)`. `Sem` itself is not yet constructed — that is tracked in
> §10.6, and until it is, the formulas carry the argument informally.

---

## 4. Self-check results (all read-only / local)

- `python3 -m py_compile` on all four `src/*.py` — clean.
- `python3 cognitive_form.py validate example.cform.json` — ✓ valid.
- `python3 cognitive_form.py reflect example.cform.json` — runs, clean JSON.
- `compose()` regression: composed form **validates against the schema**; fresh id ✓, feedback union ✓, history entry with both parent hashes ✓.
- `summarize()` smoke test with invalid + NaN rows: invalid trials excluded and counted, NaN treated as missing ✓; refusal detector catches the exact v0.2 failure string and passes clean text ✓.
- `./scripts/check-no-secrets.sh` — "nothing staged," exit 0 (nothing is staged; no commits made).
- All relative markdown links machine-checked across the repo — no dead links (the P-1 `sources/*` dead references are gone).
- JSON sanity: `example.cform.json`, `cognitive-form.schema.json` parse.
- Phrase scan: no "existence proof," no "blog ADR-023," no `sources/` refs, no "committed before the run" outside `reviews/`; the one remaining "external review" is the intentional "genuine external review … is still pending."

## 5. Needs Joshua (one line each)

1. **Push gate (Red):** everything above is uncommitted; the two already-committed-but-unpushed commits (`ae3a8f4`, `0eccdae`) should only go out together with these fixes.
2. **Re-run v0.2 clean** (hardened harness is in place); the ROADMAP section promises it.
3. **Decide `reviews/` visibility:** these review/pass files sit in the public repo's tree — commit them, move them private, or gitignore before any push.
4. **Outbox note destination:** pick its public path (`docs/` or a new `patterns/`), add it to the README layout list, add a Last-Updated footer on push.
5. **B-8:** approve the one-line GUARDRAILS rescope ("no real names *from your captures*; maintainer attribution is the exception").
6. **B-9:** pick `census` vs `census.md` convention and apply it everywhere.
7. **Theory M1/M2/M3/M9 + L-series:** spec-owner decisions (operator carriers, §6.6 S→Φ, "laws" retitle, axiom wording) — all have concrete suggested fixes in the theory review.

---

**Last Updated:** 2026-07-01
