---
# EXP front-matter — links this experiment into the public knowledge graph.
# Validate against ../schemas/experiment.schema.json.
id: EXP-000                    # NNN, unique within this lab
title: <short experiment name>
status: draft                  # draft | running | done | invalid
opened: <YYYY-MM-DD>
closed:                        # <YYYY-MM-DD> when status is done/invalid
depends_on: []                 # public A-/D- ids this relies on, e.g. [A-002, D-006]
tests: []                      # public H- ids this tests, e.g. [H-001]
updates: []                    # public D- ids this would revise
supports: []                   # public L- ids this provides evidence for
contradicts: []                # public claims this counts against (→ a CEX card)
orientation_delta:             # measured in this lab's units (labs §6); may be null
---

# EXP-000 — <title>

> One experiment. Private lab data. Only an abstracted **pattern card** ever leaves
> this repo (labs spec §7).

## Question

<The specific, answerable question. Tie it to an RQ-NNN if it maps to one.>

## Prediction (pre-registered)

<What you expect to see, written BEFORE running. Recording this before the result is
the discipline that makes a null or an adverse result meaningful — see ROADMAP.md.>

## Method

<Two conditions if it's a comparison. What varies, what's held fixed, how many
trials. Keep it reproducible.>

## Measure

<How orientation delta / a Λ proxy (labs §6.2–6.3) is scored. State the scale and who
or what judges. Prefer L = P(S|M)/C(M) or an explicit ordinal before/after.>

## Result

<What actually happened. If invalid (adapter error, contaminated input, scorer
artifact), say so and say why — an invalid run reported honestly is worth more than a
clean-looking artifact. Report direction even when the run doesn't support your
prediction.>

## What promotes (if anything)

<Does this recur across ≥3 contexts? If a stable, domain-stripped pattern emerged,
draft a pattern card (templates/pattern-card.template.md). If not, say "stays in the
lab" and why.>
